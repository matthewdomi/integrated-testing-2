## Integrated - testing

Run the command

`$yarn init -y`

Install the following

`$ yarn add -D jest`

`$ yarn add axios`

Run the command below to test

`$ yarn run jest`

In package.json

` "test": "jest" `
